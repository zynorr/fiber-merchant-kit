# FiberStore uses the API server's public demo checkout route.
# No merchant API key is exposed to the shopper browser.
# Leave empty for local Vite proxy. Set to the deployed API origin for split hosting.
# Example: https://api.fiber-merchant.example
VITE_MERCHANT_API_URL=
